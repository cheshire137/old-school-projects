<p>Welcome to Nile<%= ", #{$session[:user].first_name} #{$session[:user].last_name}" if $session[:user] %>!  <%= "(Not <a href=\"#{BaseURI}/logout.cgi\">#{$session[:user].first_name}</a>?)" if $session[:user] %>  We sell books and toys.</p>

<div id="toy_box"><%= Toy.search_form %></div>
<div id="book_box"><%= Book.search_form %></div>
<div id="login_box"><%= Person.login_form %></div>
